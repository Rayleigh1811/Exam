#cloud-config
package_upgrade: true
packages:
  - nginx
runcmd:
  - service nginx restart
{% for i in range(1, 4) %}
  - sh -c 'echo "<head><title>Phi1811: `hostname`</title> > /var/www/html/index{{i}}.html'
{% endfor %}
